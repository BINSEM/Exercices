alert('coucou monde!');
var foo = "exercices"
var bar = foo.toLowerCase();
if (foo == bar) {alert ("much")}
	else {alert ("not much")}
var teki = "COUCOR"
var semi = teki.toLowerCase();
if (teki == semi)
	{ alert ("much")}
else {alert ("not much")}

function comparchaine (str) {
	var maj = str.toUpperCase();
	var tag = "<br>";
	if (str == maj) {
	document.writeln(str+" it is much");
	}
} 
comparchaine ("blabla  blabla");

var foo = "flafla flafla";
comparchaine(foo)
comparchaine("KSDHGSKRDSH");
